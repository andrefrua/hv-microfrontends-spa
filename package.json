{
  "name": "hv-microfrontends-spa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run dev --parallel"
  },
  "dependencies": {
    "lerna": "^3.20.1"
  }
}
